openapi: '3.0.0'
info:
  title: Mock REST API Server
  version: "0.1"
  description: Testing the API Compose Framework

servers:
  - url: http://localhost:8085
    description: Test Base URL

tags:
  - name: unit-test
    description: Endpoints for unit testing
  - name: e2e-test
    description: Endpoints for E2E testing
  - name: stateless
    description: Stateless Endpoints
  - name: stateful-user
    description: Stateful Endpoints defined by user
  - name: stateful-system
    description: Stateful Endpoints defined by system

paths:
   # Stateless Endpoint
   /hello:
    get:
      summary: Return the number you input
      parameters:
        - in: query
          name: number
          schema:
            type: integer
          required: true
          description: the number to return
      responses:
        "200":
          description: OK